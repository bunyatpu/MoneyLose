import React from 'react';
import { StyleSheet, Text, View, Button } from 'react-native';

const ProfileScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor:'#2C394B'}} >
            <Text style={{color:'#ffffff'}}>ProfileScreen</Text>
        </View>
    );
};

export default ProfileScreen;